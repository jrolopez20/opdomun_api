definitions:
  Image:
    type: object
    properties:
      id:
        type: integer
      post:
        $ref: '#/definitions/Post'
      url:
        type: string
      default:
        type: integer
    required:
      - id
      - post
      - url
