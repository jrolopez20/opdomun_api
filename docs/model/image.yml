definitions:
  Image:
    type: object
    properties:
      id:
        type: uint
      post_id:
        $ref: '#/definitions/Post'
      url:
        type: string
      default:
        type: integer
      created_at:
        type: string
      updated_at:
        type: string
    required:
      - id
      - post_id
      - url
