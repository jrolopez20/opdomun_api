definitions:
  Image:
    type: object
    properties:
      id:
        type: integer
        readOnly: true
      url:
        type: string
      default:
        type: integer
    required:
      - id
      - post
      - url
