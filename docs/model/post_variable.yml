definitions:
  PostVariable:
    type: object
    properties:
      id:
        type: integer
      post_id:
        $ref: '#/definitions/Post'
      variable_id:
        $ref: '#/definitions/Variable'
      result:
        type: number
      points:
        type: number
      created_at:
        type: string
      updated_at:
        type: string
    required:
      - id
      - post_id
      - variable_id
