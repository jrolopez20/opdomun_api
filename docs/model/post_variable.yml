definitions:
  PostVariable:
    type: object
    properties:
      id:
        type: integer
      post:
        $ref: '#/definitions/Post'
      variable:
        $ref: '#/definitions/Variable'
      result:
        type: number
      points:
        type: number
    required:
      - id
      - post
      - variable
