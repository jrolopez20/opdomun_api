definitions:
  Localidad:
    type: object
    properties:
      id:
        type: integer
        readOnly: true
      municipio:
        $ref: '#/definitions/Municipio'
      title:
        type: string
    required:
      - id
      - municipio
      - title
