definitions:
  Localidad:
    type: object
    properties:
      id:
        type: integer
      municipio:
        $ref: '#/definitions/Municipio'
      title:
        type: string
    required:
      - id
      - municipio
      - title
