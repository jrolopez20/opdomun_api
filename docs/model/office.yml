definitions:
  Office:
    type: object
    properties:
      id:
        type: integer
        readOnly: true
      provincia:
        $ref: '#/definitions/Provincia'
    required:
      - id
      - provincia
