definitions:
  InfUrbana:
    type: object
    properties:
      id:
        type: integer
      postVariable:
        $ref: '#/definitions/PostVariable'
      value:
        type: integer
      title:
        type: string
    required:
      - id
      - postVariable
      - value
      - title
