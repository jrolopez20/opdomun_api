definitions:
  PostPlace:
    type: object
    properties:
      id:
        type: uint
      post_id:
        $ref: '#/definitions/Post'
      title:
        type: string
      score:
        type: number
      created_at:
        type: string
      updated_at:
        type: string
    required:
      - id
      - post_id
      - title
      - score
