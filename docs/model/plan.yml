definitions:
  Plan:
    type: object
    properties:
      id:
        type: integer
      type:
        type: string
      ranking:
        type: integer
    required:
      - id
      - type
      - ranking
