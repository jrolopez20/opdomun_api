definitions:
  HisPost:
    type: object
    properties:
      id:
        type: integer
      post_id:
        $ref: '#/definitions/Post'
      action:
        type: integer
        description: '1-Publicado, 2-Renovado, 3-Tasar'
      created_at:
        type: string
      updated_at:
        type: string
    required:
      - id
      - post_id
      - action
