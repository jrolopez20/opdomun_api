definitions:
  Subscription:
    type: object
    properties:
      id:
        type: uint
      user_id:
        $ref: '#/definitions/User'
      provincia_id:
        $ref: '#/definitions/Provincia'
      municipio:
        type: string
        description: 'List of comma separated of municipio_id'
      home_type:
        type: string
        description: 'List of comma separated of home_type_id'
      min_price:
        type: number
      max_price:
        type: number
      bedrooms:
        type: integer
      bathrooms:
        type: integer
      created_at:
        type: string
      updated_at:
        type: string
    required:
      - id
      - user_id
      - provincia_id
      - municipio
      - home_type
      - min_price
      - max_price
      - bedrooms
      - bathrooms
