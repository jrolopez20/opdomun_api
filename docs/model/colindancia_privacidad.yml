definitions:
  ColindanciaPrivacidad:
    type: object
    properties:
      id:
        type: integer
      postVariable:
        $ref: '#/definitions/PostVariable'
      relHor:
        type: integer
      relVert:
        type: integer
      tipoVia:
        type: integer
      alturaCercPer:
        type: string
      permeabilidad:
        type: integer
      altura:
        type: integer
      distancia:
        type: string
    required:
      - id
      - postVariable
      - relHor
      - relVert
      - tipoVia
      - alturaCercPer
      - permeabilidad
      - altura
      - distancia
