definitions:
  ColindanciaPrivacidad:
    type: object
    properties:
      id:
        type: integer
      post_variable_id:
        $ref: '#/definitions/PostVariable'
      rel_hor:
        type: integer
      rel_vert:
        type: integer
      tipo_via:
        type: integer
      altura_cerc_per:
        type: string
      permeabilidad:
        type: integer
      altura:
        type: integer
      distancia:
        type: string
      created_at:
        type: string
      updated_at:
        type: string
    required:
      - id
      - post_variable_id
      - rel_hor
      - rel_vert
      - tipo_via
      - altura_cerc_per
      - permeabilidad
      - altura
      - distancia
