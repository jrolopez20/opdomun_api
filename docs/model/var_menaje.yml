definitions:
  VarMenaje:
    type: object
    properties:
      id:
        type: integer
      post_id:
        $ref: '#/definitions/Post'
      exist:
        type: integer
        default: 0
        description: '0-No tiene menaje, 1-Se vende con menaje'
      created_at:
        type: string
      updated_at:
        type: string
    required:
      - id
      - post_id
      - exist
