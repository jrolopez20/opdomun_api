definitions:
  VarMenaje:
    type: object
    properties:
      id:
        type: integer
      post:
        $ref: '#/definitions/Post'
      exist:
        type: integer
        default: 0
        description: '0-No tiene menaje, 1-Se vende con menaje'
    required:
      - id
      - post
      - exist
