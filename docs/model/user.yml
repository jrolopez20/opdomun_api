definitions:
  User:
    type: object
    properties:
      id:
        type: integer
      office:
        $ref: '#/definitions/Office'
      numid:
        type: string
      email:
        type: string
      password:
        type: string
      role:
        type: string
      fullname:
        type: string
      telephone:
        type: string
      address:
        type: string
      picture:
        type: string
    required:
      - id
      - numid
      - email
      - password
      - fullname
      - role
