definitions:
  User:
    type: object
    properties:
      id:
        type: uint
      office_id:
        $ref: '#/definitions/Office'
      numid:
        type: string
      email:
        type: string
      password:
        type: string
      role:
        type: string
      fullname:
        type: string
      telephone:
        type: string
      address:
        type: string
      picture:
        type: string
      created_at:
        type: string
      updated_at:
        type: string
    required:
      - id
      - numid
      - email
      - password
      - fullname
      - role
