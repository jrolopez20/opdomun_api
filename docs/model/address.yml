definitions:
  Address:
    type: object
    properties:
      id:
        type: integer
        readOnly: true
      localidad:
        $ref: '#/definitions/Localidad'
      street:
        type: string
      coordinates:
        $ref: '#/definitions/Point'
    required:
      - id
      - coordinates
      - street
