definitions:
  PostVisit:
    type: object
    properties:
      id:
        type: integer
      post:
        $ref: '#/definitions/Post'
      total:
        type: integer
    required:
      - id
      - post
      - total
