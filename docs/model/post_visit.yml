definitions:
  PostVisit:
    type: object
    properties:
      id:
        type: integer
        readOnly: true
      post:
        $ref: '#/definitions/Post'
      total:
        type: integer
    required:
      - id
      - post
      - total
