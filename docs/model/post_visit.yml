definitions:
  PostVisit:
    type: object
    properties:
      id:
        type: integer
      post_id:
        $ref: '#/definitions/Post'
      total:
        type: integer
      created_at:
        type: string
      updated_at:
        type: string
    required:
      - id
      - post_id
      - total
