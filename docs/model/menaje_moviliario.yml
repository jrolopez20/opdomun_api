definitions:
  MenajeMoviliario:
    type: object
    properties:
      id:
        type: integer
      menage:
        $ref: '#/definitions/VarMenaje'
      moviliario:
        $ref: '#/definitions/NomMoviliario'
    required:
      - id
      - menage
      - moviliario
