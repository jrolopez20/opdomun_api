definitions:
  MenajeMoviliario:
    type: object
    properties:
      id:
        type: integer
        readOnly: true
      menage:
        $ref: '#/definitions/VarMenaje'
      moviliario:
        $ref: '#/definitions/NomMoviliario'
    required:
      - id
      - menage
      - moviliario
