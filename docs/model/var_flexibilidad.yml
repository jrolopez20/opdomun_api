definitions:
  VarFlexibilidad:
    type: object
    properties:
      id:
        type: uint
      post_id:
        $ref: '#/definitions/Post'
      area_crecimiento:
        type: number
        description: 'Max resize property'
      created_at:
        type: string
      updated_at:
        type: string
    required:
      - id
      - post_id
      - area_crecimiento
