definitions:
  EstConstructiva:
    type: object
    properties:
      id:
        type: integer
        readOnly: true
      postVariable:
        $ref: '#/definitions/PostVariable'
      displayValue:
        type: string
      title:
        type: string
    required:
      - id
      - postVariable
      - displayValue
      - title
