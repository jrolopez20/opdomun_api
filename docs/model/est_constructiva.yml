definitions:
  EstConstructiva:
    type: object
    properties:
      id:
        type: integer
      postVariable:
        $ref: '#/definitions/PostVariable'
      displayValue:
        type: string
      title:
        type: string
    required:
      - id
      - postVariable
      - displayValue
      - title
