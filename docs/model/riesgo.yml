definitions:
  Riesgo:
    type: object
    properties:
      id:
        type: integer
        readOnly: true
      postVariable:
        $ref: '#/definitions/PostVariable'
      value:
        type: integer
      type:
        type: integer
    required:
      - id
      - postVariable
      - value
