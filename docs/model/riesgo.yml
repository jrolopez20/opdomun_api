definitions:
  Riesgo:
    type: object
    properties:
      id:
        type: integer
      postVariable:
        $ref: '#/definitions/PostVariable'
      value:
        type: integer
      type:
        type: integer
    required:
      - id
      - postVariable
      - value
