definitions:
  Riesgo:
    type: object
    properties:
      id:
        type: uint
      post_variable_id:
        $ref: '#/definitions/PostVariable'
      value:
        type: integer
      type:
        type: integer
      created_at:
        type: string
      updated_at:
        type: string
    required:
      - id
      - post_variable_id
      - value
