definitions:
  Article:
    type: object
    properties:
      id:
        type: integer
        readOnly: true
      user:
        $ref: '#/definitions/User'
      title:
        type: string
      summary:
        type: string
      text:
        type: string
      picture:
        type: string
    required:
      - id
      - user
      - title
      - summary
      - text
