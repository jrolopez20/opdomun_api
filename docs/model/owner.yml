definitions:
  Owner:
    type: object
    properties:
      id:
        type: integer
      post:
        $ref: '#/definitions/Post'
      user:
        $ref: '#/definitions/User'
      fullname:
        type: string
      telephone:
        type: string
      email:
        type: string
    required:
      - id
      - post
      - fullname
      - telephone
