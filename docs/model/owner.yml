definitions:
  Owner:
    type: object
    properties:
      id:
        type: integer
      post_id:
        $ref: '#/definitions/Post'
      user_id:
        $ref: '#/definitions/User'
      fullname:
        type: string
      telephone:
        type: string
      email:
        type: string
      created_at:
        type: string
      updated_at:
        type: string
    required:
      - id
      - post_id
      - fullname
      - telephone
