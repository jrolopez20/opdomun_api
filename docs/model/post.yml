definitions:
  Post:
    type: object
    description: 'Store all post and appraisals'
    properties:
      id:
        type: integer
      user_id:
        $ref: '#/definitions/User'
      municipio_id:
        $ref: '#/definitions/Municipio'
      home_type_id:
        $ref: '#/definitions/HomeType'
      plan:
        $ref: '#/definitions/Plan'
      address:
        type: string
      price:
        type: number
      area:
        type: number
      bedrooms:
        type: integer
      bathrooms:
        type: integer
      summary:
        type: string
      evi:
        type: number
      opdo:
        type: number
      built_year:
        type: integer
      built_status:
        type: string
      sold:
        type: integer
      created_at:
        type: string
      updated_at:
        type: string
      published_at:
        type: string
      closed_at:
        type: string
    required:
      - id
      - user_id
      - municipio_id
      - home_type_id
      - address
      - area
      - bedrooms
      - bathrooms
