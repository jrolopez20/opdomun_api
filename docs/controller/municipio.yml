/provincias/{id}/municipios:
  get:
    tags:
      - Municipio
    description: List of municipios
    produces:
      - application/json
    parameters:
      - $ref: '#/parameters/id'
    responses:
      200:
        description: Success
        schema:
          type: array
          items:
            $ref: '#/definitions/Municipio'
